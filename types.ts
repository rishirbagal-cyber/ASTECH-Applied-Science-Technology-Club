
export interface TeamMember {
  name: string;
  role: string;
  image?: string;
}

export interface Domain {
  title: string;
  description: string;
  iconName: string;
}

export interface Event {
  title: string;
  date: string;
  description: string;
  image: string;
}
